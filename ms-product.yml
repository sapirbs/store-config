server:
  port: 8012

spring:
  cloud:
    consul:
      discovery:
        instanceId: product-service
        health-check-interval: 15s
      config:
        management:
          enabled: true
management:
  security:
    enabled: false
